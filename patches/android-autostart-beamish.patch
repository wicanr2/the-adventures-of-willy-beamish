diff --git a/backends/platform/android/org/scummvm/scummvm/ScummVMActivity.java b/backends/platform/android/org/scummvm/scummvm/ScummVMActivity.java
index 7288d333..3154e792 100644
--- a/backends/platform/android/org/scummvm/scummvm/ScummVMActivity.java
+++ b/backends/platform/android/org/scummvm/scummvm/ScummVMActivity.java
@@ -1106,8 +1106,12 @@ public class ScummVMActivity extends Activity {
 		final Uri intentData = getIntent().getData();
 		String[] args;
 		if (intentData == null) {
+			// Willy-CHT dedicated single-game build: boot straight into the bundled game ("beamish")
+			// instead of the launcher. (First run still shows the launcher once, because the
+			// bundled-game mass-add overrides the startup command while assets are being extracted.)
 			args = new String[]{
-				"ScummVM"
+				"ScummVM",
+				"beamish"
 			};
 		} else {
 			args = new String[]{
